<UserControl x:Class="EsoLogFormatter.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EsoLogFormatter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="Black" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button Content="Import" DockPanel.Dock="Bottom" Margin="5" Command="{Binding ImportCommand}"/>
            
                <DockPanel>
                    <Label Content="Select file: " DockPanel.Dock="Left" VerticalAlignment="Center" />
                    <local:FileSelectorView DataContext="{Binding FileSelector}"/>
                </DockPanel>
            </DockPanel>
        </Border>

        <Button Content="Process" DockPanel.Dock="Bottom"/>
        
        <Label Content="Stuff goes here" HorizontalAlignment="Center" VerticalAlignment="Center"
               FontSize="40"/>
    </DockPanel>
</UserControl>
